// import { fromJS } from 'immutable';
// import { makeSelectPlaceDomain } from '../selectors';

// const selector = makeSelectPlaceDomain();

describe('makeSelectPlaceDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
