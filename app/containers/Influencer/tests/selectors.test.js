// import { fromJS } from 'immutable';
// import { makeSelectInfluencerDomain } from '../selectors';

// const selector = makeSelectInfluencerDomain();

describe('makeSelectInfluencerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
